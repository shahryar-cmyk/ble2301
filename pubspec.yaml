name: ble2301
description: A new Flutter plugin project.
version: 0.0.8
homepage:

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.jstyle.blesdk2301.ble2301
        pluginClass: Ble2301Plugin
      ios:
        pluginClass: Ble2301Plugin
